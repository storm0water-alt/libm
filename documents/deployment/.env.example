# ===================================
# 档案管理系统 - 环境变量配置 (WSL2 优化版)
# ===================================
# 复制此文件为 .env 并修改相应的值

# ===================================
# Windows + WSL2 文件路径配置
# ===================================
# 重要：这些路径将在 Docker 中映射到 WSL2 挂载点

# 源文件目录（移动硬盘等）- Windows 格式，逗号分隔
# 示例：C:\MobileDrive,D:\BackupPDFs,E:\Documents\PDFs
SOURCE_DIRECTORIES=C:\\MobileDrive,D:\\BackupPDFs

# 档案存储目录 - Windows 格式
# 系统会将 PDF 文件存储到此目录
ARCHIVE_STORAGE_PATH=C:\\ArchiveStorage

# ===================================
# 并行导入配置
# ===================================
# 并发处理文件数量（1-10），建议根据服务器性能调整
# 高性能服务器：5-8，普通服务器：3-4，低配服务器：2-3
IMPORT_CONCURRENCY=3

# ===================================
# 数据库配置
# ===================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here_change_in_production
POSTGRES_DB=archive_management

# ===================================
# NextAuth 配置
# ===================================
# 生成方法: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_with_openssl
NEXTAUTH_URL=http://localhost:3000

# ===================================
# Meilisearch 配置
# ===================================
# 生成方法: openssl rand -base64 32
MEILISEARCH_MASTER_KEY=your_meilisearch_key_here_generate_with_openssl

# ===================================
# 应用配置
# ===================================
# 服务端口（默认 3000）
APP_PORT=3000

# ===================================
# Docker 数据持久化路径（WSL2 优化）
# ===================================
# 这些是 WSL2 系统中的实际路径
# Windows C:\ArchiveStorage → /mnt/c/ArchiveStorage → ./data/archives
# Windows C:\MobileDrive → /mnt/c/MobileDrive → ./data/mobile-drive  
# Windows D:\BackupPDFs → /mnt/d/BackupPDFs → ./data/backup-pdfs

# 注意：实际部署时会自动在 Docker Compose 中映射这些路径
ARCHIVE_STORAGE_LINUX=/app/archives
MOBILE_DRIVE_PATH=/app/source/mobile-drive
BACKUP_PDFS_PATH=/app/source/backup-pdfs
TEMP_PROCESSING_PATH=/app/temp

# ===================================
# 高级配置（可选）
# ===================================
# 启用 WSL2 性能优化（true/false）
ENABLE_WSL2_OPTIMIZATION=true

# 设置最大内存使用（MB），0 表示不限制
MAX_MEMORY_USAGE=2048

# 大文件处理阈值（MB）
LARGE_FILE_THRESHOLD=50
MEDIUM_FILE_THRESHOLD=10